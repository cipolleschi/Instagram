import { View } from 'react-native';

// Placeholder component - the tab press is intercepted in _layout.tsx
// to navigate to the modal /new route instead
export default function NewPlaceholder() {
  return <View />;
}

